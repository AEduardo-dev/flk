# flk switch/refresh

Enable automatic environment switching/refresh when moving between projects.

```bash
eval "$(flk hook bash)"   # add to your shell profile
```

**Notes**
- Supported shells: bash, zsh, fish.
- Requires the flk shell hook to be sourced.
