# flk export

Export the current flake configuration.

```bash
flk export --format docker
flk export --format podman
flk export --format json
```

**Options**
- `--format <FORMAT>`: one of `docker`, `podman`, `json`
